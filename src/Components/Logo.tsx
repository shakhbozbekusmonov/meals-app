import logo from "../Assets/img/logo.png";

const Logo = () => {
    return (
        <img
            src={logo}
            width={100}
            height={50}
            alt='Meals and recipe logo img'
        />
    );
};

export default Logo;
